var tetanusNeonatorum = (function(){
	$(function(){

        caseEdit.toggerAddress();
        initMethod();
	});

    /**
     * 页面加载时需要执行的函数
     */
    function initMethod() {
        $("input[name='attackCondition.aroundInvasion']").change(function(){
            toggleOther('attackCondition.aroundInvasion','aroundInvasionCount',2);
        });
        toggleOther('attackCondition.aroundInvasion','aroundInvasionCount',2);
        toggleOther('attackCondition.tetanusToxoidFlg','tetanusToxoidFlgFromAddrd',1);
        toggleOther('attackCondition.hospitalFlg','hospitalFlgFromAddrd',1);
    }

})();
